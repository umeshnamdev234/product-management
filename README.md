# product-management
A RESTful Product Management application built with Spring Boot featuring JWT-based authentication, role-based authorization (VIEW/EDIT), product CRUD operations, centralized exception handling, request validation, PostgreSQL persistence, and unit/integration testing.
